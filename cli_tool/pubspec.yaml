name: lang_cli
description: Desktop CLI tool for language learning utilities
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.5.0
  http: ^1.2.0
  html: ^0.15.0
  yaml: ^3.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  lang: main